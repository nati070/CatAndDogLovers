import React from 'react';

const Error = () => {
    return (
        <div className='bg-img-error'>
            Something wrong, cannot connect to server, try to upload your own image
        </div>
    );
};

export default Error;